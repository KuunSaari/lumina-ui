import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'
import { withInstall } from '@lumina-ui/utils'

export const LuButton = withInstall(Button)
export const LuButtonGroup = withInstall(ButtonGroup)

export * from './types'